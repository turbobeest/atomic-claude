{
  "version": "2.1.0",
  "description": "Directories included in runtime sparse-checkout install. Everything at the top level deploys; dev/ does not.",
  "cones": {
    "required": [
      "core",
      "phases",
      "orchestration",
      "config",
      "initialization",
      "agents",
      "audits"
    ],
    "optional": {
      "dashboard": "Real-time monitoring UI (requires Node.js 18+)"
    }
  },
  "excluded_dev_only": [
    "dev"
  ]
}
