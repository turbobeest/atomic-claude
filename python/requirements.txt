# ATOMIC CLAUDE - Python Implementation
# Core dependencies for orchestration system

# No external dependencies needed for core functionality!
# Python 3.9+ stdlib provides everything:
# - json (JSON parsing)
# - subprocess (CLI invocations)
# - pathlib (file operations)
# - argparse (CLI parsing)
# - typing (type hints)

# Optional: Enhanced functionality
# pyyaml>=6.0  # If we need YAML config support
# rich>=13.0   # If we want fancy terminal output like bash colors
